const container = document.getElementById("container");



for (let row = 1; row <= 100*100; row++) {
    
        const divs = document.createElement("div");
        container.appendChild(divs);
    
}